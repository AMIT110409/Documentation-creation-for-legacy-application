# PayPalCredentials.php

**Path**: `plugins\PayPalPlugin\src\Entity\PayPalCredentials.php`

## Summary
This PHP code defines an entity class `PayPalCredentials` that represents a set of credentials for PayPal payment processing, implementing the `PayPalCredentialsInterface`. The class encapsulates properties such as `id`, `paymentMethod`, `accessToken`, `creationTime`, and `expirationTime`, which are mapped to a database table using Doctrine ORM annotations. The purpose of this entity is to store and manage PayPal credentials for payment methods in an e-commerce system, specifically within the Sylius framework.

## Classes
- `PayPalCredentials`

## Function Details

### `id`


### `paymentMethod`


### `accessToken`


### `creationTime`


### `expirationTime`


### `isExpired`


