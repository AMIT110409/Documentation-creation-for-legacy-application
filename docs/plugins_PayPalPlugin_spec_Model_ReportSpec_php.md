# ReportSpec.php

**Path**: `plugins\PayPalPlugin\spec\Model\ReportSpec.php`

## Summary
This PHP specification file defines the behavior of the `Report` class in the context of the Sylius PayPal plugin, specifically testing its ability to store and retrieve report content and file names. The `ReportSpec` class extends `ObjectBehavior` to verify that the `Report` object is correctly constructed and exposes its content and file name through respective methods. This specification exists to ensure the `Report` class functions as expected, providing a foundation for reliable report generation and management within the PayPal plugin.

## Classes
- `ReportSpec`

## Function Details

### `let`


### `it_has_content`


### `it_has_a_file_name`


